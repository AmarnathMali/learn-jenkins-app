pipeline {
    agent any
    stages {
        
        stage("Build-Docker-image-nightly") {
            steps {
                sh 'docker build -t my-playwright-image .'
            }
        }
    }
}